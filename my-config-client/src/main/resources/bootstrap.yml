spring:
  application:
    name: my-client
  cloud:
    config:
      uri: http://localhost:8888  # 配置中心服务的地址
#      name: cloud-config #要读取的配置文件application属性
#      profile: ${config.profile:dev} # 要读取的配置文件profile属性,默认是dev
